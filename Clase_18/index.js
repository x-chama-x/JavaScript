"use strict"

// Medir y acceder a un arreglo en JS

var platillos = ["ceviche", "tacos","pasta"]; 

console.log("hay "+ platillos.length +" platillos en el menu"); // platillos.length --> cuenta los elementos del arreglo

var platillo = platillos[0]; // acceder a un elemento del arreglo, en este caso ceviche
console.log("el platillo seleccionado es:", platillo);